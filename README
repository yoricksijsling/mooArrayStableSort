TODO:
- Add default sort function (is that ever useful?)
- Add test
- Add demo

Adds a stable sort algorithm for all browsers


HOW TO USE

var myArray = [1, 5, 3]

myArray.stableSort(function(a,b) { });

This uses a custom algorithm for browsers which do not have a native stable algorithm.

